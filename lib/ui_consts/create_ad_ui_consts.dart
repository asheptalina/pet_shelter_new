import 'package:flutter/material.dart';

class CreateAdUIConstants {
  static const verticalPadding = 24.0;

  static const photoButtonWidthCof = 0.92;

  static const mapDefaultZoom = 15.0;
  static const mapMarkerSize = 75.0;
  static const currentLocationButtonPaddings = EdgeInsets.all(16.0);

  static const petTypeSelectorsSpacing = 12.0;
}